<Page x:Class="prakt15.pages.SelectTags"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:prakt15.pages"
      mc:Ignorable="d" 
      Background="#dfeaeb"
      d:DesignHeight="450" d:DesignWidth="800"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      DataContext="{Binding RelativeSource={RelativeSource Self}}"

      Title="SelectTags" Loaded="Page_Loaded">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="1">
            <ComboBox x:Name="TagsCB" Background="#00C4B4" materialDesign:HintAssist.Hint="Теги" Margin="0 5" DisplayMemberPath="Name" ItemsSource="{Binding tagsService.Tags}" SelectedItem="{Binding tags, Mode=TwoWay}"></ComboBox>
            <Button x:Name="Add" Background="#00C4B4" Click="Add_Click">Добавить</Button>
            <Button x:Name="Back" Background="#00C4B4" Click="Back_Click">Назад</Button>
        </StackPanel>
    </Grid>
</Page>
